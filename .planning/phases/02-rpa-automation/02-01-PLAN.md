---
phase: 02-rpa-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - uipath/Main.xaml
  - uipath/config.json.example
  - uipath/.gitignore
  - uipath/README.md
  - uipath/watch/.gitkeep
  - uipath/watch/processed/.gitkeep
  - uipath/rpa_screenshots/.gitkeep
  - uipath/test_files/parcel_damaged_2026.txt
  - uipath/test_files/missing_package_2026.txt
  - uipath/test_files/duplicate_test.txt
autonomous: false
requirements:
  - RPA-01
  - RPA-02
  - RPA-03
  - RPA-04
  - RPA-05
  - RPA-06
  - RPA-07
user_setup:
  - service: uipath_studio
    why: "Author and run the RPA workflow"
    install:
      - "Download UiPath Studio Community Edition from uipath.com/community"
      - "Sign in with personal email"
      - "Create new Process project named DHL_IRRS_RPA at repo root /uipath"
  - service: google_drive
    why: "Watch folder source per RPA-01"
    install:
      - "Install Google Drive for Desktop"
      - "Sign in; let it mount Drive as a local letter (e.g. G:)"
      - "Create folder G:\\My Drive\\irrs-watch (path goes in config.json)"
  - service: gmail_smtp
    why: "Summary email per RPA-07"
    env_vars:
      - name: email_app_password (in config.json)
        source: "Gmail -> Manage your Google Account -> Security -> 2-Step Verification -> App passwords -> Generate (Mail / Other: IRRS-RPA)"
    dashboard_config:
      - task: "Enable 2-Step Verification on Gmail account if not already enabled"
        location: "https://myaccount.google.com/security"

must_haves:
  truths:
    - "Bot reads new .txt files from a configured Google Drive watch folder on each run"
    - "Bot computes SHA-256 of file content matching irrs-backend/utils/dedup.py exactly"
    - "Bot calls POST /api/incidents/check-duplicate before creating; on is_duplicate=true it logs skip and moves the file to processed/"
    - "Bot creates incident via multipart POST /api/incidents with title=filename_no_ext, description=file_content, priority=medium, category=other, source=rpa, content_hash=sha256, files=<file>"
    - "Bot advances created incident from draft to reviewed via PATCH /api/incidents/{id}/status with body {\"status\":\"reviewed\"}"
    - "Bot writes one /api/logs entry per action (create, status_update, skip_duplicate, error) with shared run_id per run"
    - "On any per-file exception bot saves a screenshot to rpa_screenshots/ and posts an error log with screenshot_path"
    - "After processing all files bot sends a plain-text summary email with Created/Duplicates/Failed counts via Gmail SMTP"
    - "After successful or skip handling each file is moved to the processed/ subfolder"
    - "Failure of one file does not abort the run — bot continues to the next file"
  artifacts:
    - path: "uipath/Main.xaml"
      provides: "UiPath workflow root with Initialize, For Each File loop with Try/Catch, Finalize sequences"
      contains: "Main.xaml"
    - path: "uipath/config.json.example"
      provides: "Template for runtime config (api_base_url, rpa_username, rpa_password, watch_folder, processed_folder, screenshots_folder, email_*)"
      contains: "api_base_url"
    - path: "uipath/.gitignore"
      provides: "Excludes config.json, screenshots, processed/ from git"
      contains: "config.json"
    - path: "uipath/README.md"
      provides: "Setup steps: install Studio, configure config.json, run workflow, demo script"
      contains: "config.json"
    - path: "uipath/test_files/parcel_damaged_2026.txt"
      provides: "Demo input file 1"
      contains: "parcel"
    - path: "uipath/test_files/missing_package_2026.txt"
      provides: "Demo input file 2"
      contains: "missing"
    - path: "uipath/test_files/duplicate_test.txt"
      provides: "Demo file used twice to trigger skip path"
      contains: "duplicate"
  key_links:
    - from: "uipath/Main.xaml (Initialize)"
      to: "POST /api/auth/login"
      via: "HTTP Request activity with body {username,password} from config.json -> stores response.data.access_token"
      pattern: "/api/auth/login"
    - from: "uipath/Main.xaml (For Each File body)"
      to: "POST /api/incidents/check-duplicate"
      via: "HTTP Request with Authorization Bearer header and JSON body {content_hash}"
      pattern: "/api/incidents/check-duplicate"
    - from: "uipath/Main.xaml (For Each File body)"
      to: "POST /api/incidents (multipart/form-data)"
      via: "HTTP Request with body parameters + file attachment field name files"
      pattern: "/api/incidents"
    - from: "uipath/Main.xaml (For Each File body)"
      to: "PATCH /api/incidents/{id}/status"
      via: "HTTP Request with JSON body {\"status\":\"reviewed\"}"
      pattern: "/status"
    - from: "uipath/Main.xaml (every action and Catch)"
      to: "POST /api/logs"
      via: "HTTP Request JSON body LogEntry; run_id constant per run"
      pattern: "/api/logs"
    - from: "uipath/Main.xaml (Finalize)"
      to: "Gmail SMTP smtp.gmail.com:587 TLS"
      via: "Send SMTP Mail Message with credentials from config.json"
      pattern: "smtp.gmail.com"
---

<objective>
Build the UiPath RPA bot that closes Phase 2 RPA requirements RPA-01 through RPA-07. The bot polls a Google Drive watch folder, deduplicates by SHA-256 against the existing FastAPI backend, creates incidents with file attached, advances them to reviewed, logs every action with a shared run_id, captures screenshots on failure, moves processed files to processed/, and emails a summary after each run.

Purpose: Satisfy the assignment's mandatory RPA section so DHL incidents flow from Drive into the IRRS web app without manual re-entry.

Output: A self-contained uipath/ workspace inside the repo containing Main.xaml, config.json.example, .gitignore, README.md, demo test files, and empty processed/ + rpa_screenshots/ folders. No backend or frontend code is changed by this plan.
</objective>

<execution_context>
@/Users/aflakhamjad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aflakhamjad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-rpa-automation/02-CONTEXT.md
@.planning/phases/02-rpa-automation/02-RESEARCH.md
@CLAUDE.md
@irrs-backend/routers/incidents.py
@irrs-backend/routers/logs.py
@irrs-backend/routers/auth.py
@irrs-backend/utils/dedup.py
@irrs-backend/seed.py

<interfaces>
<!-- Backend contracts the bot must hit. Verified from source code. -->

POST /api/auth/login
  Body (JSON): { "username": "rpa_bot", "password": "rpabot123" }
  Response: { "success": true, "data": { "access_token": "eyJ...", "token_type": "bearer", "user": {...} } }
  Extract: data.access_token

POST /api/incidents/check-duplicate
  Headers: Authorization: Bearer {token}
  Body (JSON): { "content_hash": "<sha256_hex_lowercase>" }
  Response: { "success": true, "data": { "is_duplicate": bool, "existing_incident_id": string|null } }

POST /api/incidents
  Headers: Authorization: Bearer {token}
  Body Format: multipart/form-data
  Form fields:
    title         (string)  required
    description   (string)  required
    priority      (string)  required  -> "medium"
    category      (string)  required  -> "other"
    source        (string)  optional  -> "rpa"
    content_hash  (string)  optional  -> sha256 hex
    raw_content   (string)  optional  -> ""
    tags          (string)  optional  -> ""
  File field:
    files         (UploadFile, plural field name) -> attach the .txt file
  Response: { "success": true, "data": { "incident": { "id": "<uuid>", "status": "draft", ... } } }
  Extract: data.incident.id

PATCH /api/incidents/{incident_id}/status
  Headers: Authorization: Bearer {token}
  Body (JSON): { "status": "reviewed" }
  Response: { "success": true, "data": { "incident": {...} } }

POST /api/logs
  Headers: Authorization: Bearer {token}
  Body (JSON, LogEntry):
    {
      "run_id": "<uuid>",          // shared across whole run
      "action": "create" | "skip_duplicate" | "status_update" | "error",
      "file_name": "<filename.txt>" | null,
      "incident_id": "<uuid>" | null,
      "status": "success" | "skipped" | "failed",
      "message": "<human readable>",
      "screenshot_path": "<relative path>" | null
    }
  Response: { "success": true, "data": { "log": {...} } }

SHA-256 reference (irrs-backend/utils/dedup.py):
  hashlib.sha256(content.encode("utf-8")).hexdigest()  // lowercase hex, no separators

Seeded RPA user (irrs-backend/seed.py): username=rpa_bot password=rpabot123 role=rpa_bot
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold uipath/ workspace and config template</name>
  <files>
    uipath/.gitignore
    uipath/config.json.example
    uipath/README.md
    uipath/watch/.gitkeep
    uipath/watch/processed/.gitkeep
    uipath/rpa_screenshots/.gitkeep
    uipath/test_files/parcel_damaged_2026.txt
    uipath/test_files/missing_package_2026.txt
    uipath/test_files/duplicate_test.txt
  </files>
  <read_first>
    .planning/phases/02-rpa-automation/02-CONTEXT.md
    .planning/phases/02-rpa-automation/02-RESEARCH.md
    irrs-backend/seed.py
    CLAUDE.md
  </read_first>
  <action>
Create the UiPath workspace skeleton at the repo root in a new `uipath/` folder. NO Next.js or backend file is touched.

1. Create `uipath/.gitignore` with exactly these lines (one per line):
```
config.json
rpa_screenshots/*.png
watch/processed/*
.local/
*.xaml.backup
project.json.local
```

2. Create `uipath/config.json.example` with this exact JSON (use real placeholder strings, NOT comments — JSON disallows comments):
```json
{
  "api_base_url": "http://localhost:8000/api",
  "rpa_username": "rpa_bot",
  "rpa_password": "rpabot123",
  "watch_folder": "G:\\My Drive\\irrs-watch",
  "processed_folder": "G:\\My Drive\\irrs-watch\\processed",
  "screenshots_folder": "C:\\path\\to\\repo\\uipath\\rpa_screenshots",
  "email_smtp_host": "smtp.gmail.com",
  "email_smtp_port": 587,
  "email_from": "your.gmail@gmail.com",
  "email_to": "your.gmail@gmail.com",
  "email_app_password": "xxxx xxxx xxxx xxxx"
}
```

3. Create empty placeholder dirs by writing `.gitkeep` files (zero bytes) at:
   - `uipath/watch/.gitkeep`
   - `uipath/watch/processed/.gitkeep`
   - `uipath/rpa_screenshots/.gitkeep`

4. Create three demo `.txt` test files. Their EXACT contents:

`uipath/test_files/parcel_damaged_2026.txt`:
```
Parcel damaged on arrival at KL hub on 2026-05-04. Outer box crushed, inner content (laptop) appears intact but cosmetic scuffs reported by customer. Reference DHL waybill 8492-1773-22.
```

`uipath/test_files/missing_package_2026.txt`:
```
Customer reports missing package, DHL waybill 7711-2204-09. Last scan was at JHB sorting facility on 2026-05-02. No further movement events recorded after that timestamp.
```

`uipath/test_files/duplicate_test.txt`:
```
Duplicate test fixture — copy this file into the watch folder twice across two bot runs to demonstrate RPA-02 dedup skip path. Content must remain byte-identical between runs.
```

5. Create `uipath/README.md` documenting the setup. It MUST contain (use these exact section headings):
   - `# DHL IRRS — UiPath RPA Bot`
   - `## Prerequisites` — UiPath Studio Community, Google Drive for Desktop, Gmail App Password, IRRS backend running on port 8000
   - `## Setup` — copy config.json.example to config.json, fill in credentials, copy demo files from uipath/test_files/ into the Drive watch folder
   - `## Running the bot` — open Main.xaml in Studio, click Run; bot reads config.json from same folder as Main.xaml
   - `## Demo script` — explain: run once with 3 files (3 created); copy duplicate_test.txt back to watch folder; run again (1 skip); place malformed file (e.g., zero-byte) to trigger error path; check Logs page in web app at http://localhost:3000/logs
   - `## File field name reminder` — multipart attachment field name MUST be `files` (plural) per FastAPI signature
   - `## SHA-256 contract` — UTF-8 bytes -> SHA256 -> lowercase hex, matching irrs-backend/utils/dedup.py

Reference: backend contract in irrs-backend/routers/incidents.py and seeded rpa_bot credentials in irrs-backend/seed.py.
  </action>
  <verify>
    <automated>test -f uipath/.gitignore && test -f uipath/config.json.example && test -f uipath/README.md && test -f uipath/test_files/parcel_damaged_2026.txt && test -f uipath/test_files/missing_package_2026.txt && test -f uipath/test_files/duplicate_test.txt && test -f uipath/watch/.gitkeep && test -f uipath/watch/processed/.gitkeep && test -f uipath/rpa_screenshots/.gitkeep && grep -q '"api_base_url": "http://localhost:8000/api"' uipath/config.json.example && grep -q '"rpa_username": "rpa_bot"' uipath/config.json.example && grep -q '"email_smtp_host": "smtp.gmail.com"' uipath/config.json.example && grep -q '^config.json$' uipath/.gitignore && grep -q "DHL IRRS" uipath/README.md && grep -q "files" uipath/README.md && grep -q "SHA-256" uipath/README.md && python -c "import json; json.load(open('uipath/config.json.example'))"</automated>
  </verify>
  <acceptance_criteria>
    - File `uipath/.gitignore` exists and contains line `config.json` (exact match anchored to start of line)
    - File `uipath/config.json.example` exists, parses as valid JSON, contains key `api_base_url` with value `http://localhost:8000/api`
    - File `uipath/config.json.example` contains key `rpa_username` with value `rpa_bot` and `email_smtp_host` with value `smtp.gmail.com`
    - File `uipath/README.md` contains heading text `# DHL IRRS — UiPath RPA Bot`
    - File `uipath/README.md` contains the literal string `files` (the plural attachment field name)
    - File `uipath/README.md` contains the literal string `SHA-256`
    - Files `uipath/test_files/parcel_damaged_2026.txt`, `uipath/test_files/missing_package_2026.txt`, `uipath/test_files/duplicate_test.txt` all exist and are non-empty
    - Files `uipath/watch/.gitkeep`, `uipath/watch/processed/.gitkeep`, `uipath/rpa_screenshots/.gitkeep` all exist
  </acceptance_criteria>
  <done>UiPath workspace scaffold and demo fixtures committed. Authoring task can begin against this skeleton.</done>
</task>

<task type="auto">
  <name>Task 2: Author Main.xaml UiPath workflow (Initialize -> For Each File Try/Catch -> Finalize)</name>
  <files>
    uipath/Main.xaml
    uipath/project.json
  </files>
  <read_first>
    .planning/phases/02-rpa-automation/02-CONTEXT.md
    .planning/phases/02-rpa-automation/02-RESEARCH.md
    irrs-backend/routers/auth.py
    irrs-backend/routers/incidents.py
    irrs-backend/routers/logs.py
    irrs-backend/utils/dedup.py
    uipath/config.json.example
    uipath/README.md
  </read_first>
  <action>
Author the UiPath workflow root file `uipath/Main.xaml` and minimal `uipath/project.json`. The XAML must implement the exact sequence below. Generate well-formed XAML using `Activity` root with `xmlns="http://schemas.microsoft.com/winfx/2006/xaml"` and standard UiPath namespace declarations (xmlns:ui for UiPath.Core.Activities, xmlns:hc for UiPath.WebAPI.Activities, xmlns:mail for UiPath.Mail.SMTP.Activities, xmlns:s for clr-namespace:System;assembly=mscorlib). Activity names must be human-readable and match the labels below.

`uipath/project.json` must declare dependencies on these UiPath packages (use latest stable available in Community Studio, but pin versions in the JSON):
- UiPath.System.Activities
- UiPath.WebAPI.Activities
- UiPath.UIAutomation.Activities
- UiPath.Mail.Activities

Use this exact targetFramework: `Windows`, projectVersion `1.0.0`, projectType `Workflow`, name `DHL_IRRS_RPA`, main entry `Main.xaml`.

WORKFLOW STRUCTURE — implement EXACTLY this in Main.xaml as nested activities:

PHASE A — INITIALIZE (Sequence "Initialize"):
1. `Read Text File` — path = `Path.Combine(Environment.CurrentDirectory, "config.json")` -> output variable `configRaw` (String)
2. `Deserialize JSON` — input `configRaw` -> output `config` (JObject)
3. `Assign` — `runId = Guid.NewGuid().ToString()` (String)
4. `Assign` — `created = 0`, `skipped = 0`, `failed = 0` (Int32 counters)
5. `If` — `Not Directory.Exists(config("processed_folder").ToString())` -> Then: `Create Directory` with that path. Same for `screenshots_folder`.
6. `HTTP Request` (auth) — Endpoint = `config("api_base_url").ToString() & "/auth/login"`, Method = POST, Body Format = `application/json`, Body = `"{""username"":""" & config("rpa_username") & """,""password"":""" & config("rpa_password") & """}"`. Output -> `loginResponseText` (String).
7. `Deserialize JSON` -> `loginResponse` (JObject). `Assign` -> `token = loginResponse("data")("access_token").ToString()`.

PHASE B — FOR EACH FILE (For Each `currentFile` In `Directory.GetFiles(config("watch_folder").ToString(), "*.txt")`):
Wrap the entire body in a `Try Catch`. The Try block contains:
1. `Read Text File` -> `fileContent` (String).
2. `Assign` -> `fileName = Path.GetFileName(currentFile)`, `titleNoExt = Path.GetFileNameWithoutExtension(currentFile)`.
3. `Invoke Code` (language: VB) computing SHA-256:
```vb
Dim bytes As Byte() = System.Text.Encoding.UTF8.GetBytes(fileContent)
Dim hash As Byte() = System.Security.Cryptography.SHA256.Create().ComputeHash(bytes)
sha256_result = System.BitConverter.ToString(hash).Replace("-","").ToLower()
```
   In arguments: `fileContent` (In, String). Out arguments: `sha256_result` (Out, String).
4. `HTTP Request` (check-duplicate) — POST `{api_base_url}/incidents/check-duplicate`, JSON body `{"content_hash":"<sha256_result>"}`, header `Authorization: Bearer {token}`. Output -> `dupResponseText`.
5. `Deserialize JSON` -> `dupResponse`. `Assign` -> `isDuplicate = CBool(dupResponse("data")("is_duplicate"))`.
6. `If isDuplicate`:
   - Then branch:
     a. `HTTP Request` POST `/logs` with JSON body:
        `{"run_id":"<runId>","action":"skip_duplicate","file_name":"<fileName>","incident_id":null,"status":"skipped","message":"Duplicate content hash within 14-day window","screenshot_path":null}` (Authorization header included).
     b. `Assign` -> `skipped = skipped + 1`.
     c. `Move File` -> destination = `Path.Combine(config("processed_folder").ToString(), fileName)`.
   - Else branch:
     a. `HTTP Request` POST `/incidents` — Body Format = `multipart/form-data`. Body parameters (text fields): `title=titleNoExt`, `description=fileContent`, `priority="medium"`, `category="other"`, `source="rpa"`, `content_hash=sha256_result`, `raw_content=""`, `tags=""`. Attachments: file path = `currentFile`, attachment field name MUST be `files` (plural). Header `Authorization: Bearer {token}`. Output -> `createResponseText`.
     b. `Deserialize JSON` -> `createResponse`. `Assign` -> `incidentId = createResponse("data")("incident")("id").ToString()`.
     c. `HTTP Request` POST `/logs`: `{"run_id":"<runId>","action":"create","file_name":"<fileName>","incident_id":"<incidentId>","status":"success","message":"Incident created","screenshot_path":null}`.
     d. `HTTP Request` PATCH `/incidents/{incidentId}/status` — Body Format = `application/json`, Body = `{"status":"reviewed"}`. Header `Authorization: Bearer {token}`.
     e. `HTTP Request` POST `/logs`: `{"run_id":"<runId>","action":"status_update","file_name":"<fileName>","incident_id":"<incidentId>","status":"success","message":"Advanced draft -> reviewed","screenshot_path":null}`.
     f. `Assign` -> `created = created + 1`.
     g. `Move File` -> destination = `Path.Combine(config("processed_folder").ToString(), fileName)`.

The `Catch` block (catches `System.Exception ex`):
1. `Assign` -> `screenshotPath = Path.Combine(config("screenshots_folder").ToString(), runId & "_" & fileName & ".png")`.
2. `Take Screenshot` -> save to `screenshotPath`. (UiPath activity: TakeScreenshot, output saved via Save Image File activity.)
3. `HTTP Request` POST `/logs` with body:
   `{"run_id":"<runId>","action":"error","file_name":"<fileName>","incident_id":null,"status":"failed","message":"<ex.Message escaped for JSON>","screenshot_path":"<screenshotPath>"}`.
   The exception message must be JSON-escaped (replace `"` with `\"`, replace newlines with `\n`).
4. `Assign` -> `failed = failed + 1`.
The Try-Catch must NOT rethrow — the For Each loop continues to the next file.

PHASE C — FINALIZE (Sequence after the For Each):
1. `Assign` -> `summaryBody = "DHL IRRS RPA Run Summary" & vbCrLf & "Run ID: " & runId & vbCrLf & "Timestamp: " & DateTime.UtcNow.ToString("yyyy-MM-dd HH:mm:ss") & " UTC" & vbCrLf & vbCrLf & "Created: " & created.ToString() & vbCrLf & "Duplicates: " & skipped.ToString() & vbCrLf & "Failed: " & failed.ToString()`.
2. `Send SMTP Mail Message` — Server = `config("email_smtp_host").ToString()`, Port = `CInt(config("email_smtp_port"))`, EnableSsl = True (TLS via STARTTLS — UiPath uses SecureConnection = "Auto" or equivalent), Email = `config("email_from").ToString()`, Password = `config("email_app_password").ToString()`, To = `config("email_to").ToString()`, Subject = `"DHL IRRS RPA Run Summary — " & runId`, Body = `summaryBody`.

CRITICAL CONSTRAINTS:
- The token variable must be declared at the workflow root scope so it is accessible inside the Catch block (Pitfall 4).
- The Try/Catch is INSIDE the For Each — one Try/Catch per file (Pitfall, anti-pattern: never wrap the whole loop in a single Try).
- Multipart attachment field name is `files` (plural), not `file` (Pitfall 1, Open Question 2).
- Do NOT manually set `Content-Type` for the multipart request — UiPath handles boundary generation.
- All `/logs` POSTs include `Authorization: Bearer {token}` header (Pitfall 4).
- Token is fetched once at run start (Pitfall 2 noted as acceptable for demo).

Use UiPath idioms (variables panel scope, In/Out arg directions). Generate the XAML directly — the XML must be parseable as XAML. Use VisualBasic expressions in `Expression` attributes, e.g. `[currentFile]`, `[token]`. If you cannot author every UiPath-specific attribute byte-perfectly, prioritize: (a) syntactically valid XML/XAML, (b) correct activity names and nesting per the structure above, (c) all variables and arguments declared, (d) all endpoint URLs, body strings, and header values matching the spec exactly. The user will open the file in UiPath Studio to confirm the visual layout.
  </action>
  <verify>
    <automated>test -f uipath/Main.xaml && test -f uipath/project.json && python -c "import xml.etree.ElementTree as ET; ET.parse('uipath/Main.xaml')" && grep -q '/auth/login' uipath/Main.xaml && grep -q '/incidents/check-duplicate' uipath/Main.xaml && grep -q '/incidents' uipath/Main.xaml && grep -q '/status' uipath/Main.xaml && grep -q '/logs' uipath/Main.xaml && grep -q 'multipart/form-data' uipath/Main.xaml && grep -q 'SHA256' uipath/Main.xaml && grep -q 'Bearer' uipath/Main.xaml && grep -q 'reviewed' uipath/Main.xaml && grep -q 'skip_duplicate' uipath/Main.xaml && grep -q 'status_update' uipath/Main.xaml && grep -q 'TryCatch\|Try Catch\|Catch' uipath/Main.xaml && grep -q 'TakeScreenshot\|Take Screenshot\|Screenshot' uipath/Main.xaml && grep -q 'SmtpMail\|SMTP\|Send SMTP' uipath/Main.xaml && grep -q 'smtp.gmail.com\|email_smtp_host' uipath/Main.xaml && python -c "import json; p=json.load(open('uipath/project.json')); assert p['name']=='DHL_IRRS_RPA'" && grep -q 'UiPath.WebAPI.Activities' uipath/project.json && grep -q 'UiPath.Mail.Activities' uipath/project.json</automated>
  </verify>
  <acceptance_criteria>
    - File `uipath/Main.xaml` exists and is parseable as XML (no syntax errors)
    - File `uipath/Main.xaml` contains literal string `/auth/login`
    - File `uipath/Main.xaml` contains literal string `/incidents/check-duplicate`
    - File `uipath/Main.xaml` contains literal string `/status` (the status PATCH endpoint)
    - File `uipath/Main.xaml` contains literal string `/logs`
    - File `uipath/Main.xaml` contains literal string `multipart/form-data`
    - File `uipath/Main.xaml` contains literal string `SHA256`
    - File `uipath/Main.xaml` contains literal string `Bearer` (Authorization header pattern)
    - File `uipath/Main.xaml` contains literal string `reviewed` (target status value)
    - File `uipath/Main.xaml` contains literal strings `skip_duplicate` and `status_update` (action names)
    - File `uipath/Main.xaml` contains a Try/Catch construct (matches `Catch` substring)
    - File `uipath/Main.xaml` contains a TakeScreenshot or equivalent screenshot activity reference
    - File `uipath/Main.xaml` contains an SMTP mail activity reference (SmtpMail, SMTP, or Send SMTP)
    - File `uipath/project.json` exists, parses as valid JSON, has key `name` equal to `DHL_IRRS_RPA`
    - File `uipath/project.json` declares dependencies on `UiPath.WebAPI.Activities` and `UiPath.Mail.Activities`
  </acceptance_criteria>
  <done>Main.xaml authored with the full Initialize -> For Each (Try/Catch per file) -> Finalize structure, all five backend endpoints invoked, screenshot path captured on error, summary email sent at end. Workflow opens in UiPath Studio without parser errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual end-to-end bot run (UiPath Studio + live backend)</name>
  <files>
    uipath/config.json
    uipath/watch/parcel_damaged_2026.txt
    uipath/watch/missing_package_2026.txt
    uipath/watch/duplicate_test.txt
  </files>
  <read_first>
    uipath/README.md
    uipath/Main.xaml
    uipath/config.json.example
    .planning/ROADMAP.md
  </read_first>
  <action>
HUMAN-VERIFY CHECKPOINT — the executor presents these instructions to the user, who runs the bot in UiPath Studio. The executor does not run UiPath itself.

WHAT WAS BUILT (briefing for the user):
The complete UiPath bot in `uipath/Main.xaml` is now in the repo: it reads config.json, logs into /api/auth/login, scans `watch_folder` for *.txt files, computes SHA-256, calls /api/incidents/check-duplicate, creates incidents with multipart upload, advances draft -> reviewed, logs every action with a shared run_id, screenshots failures, moves processed files, and emails a summary. This is the only verification mode for the workflow — Main.xaml is XAML, not executable from CI.

ONE-TIME SETUP (do once, before first run):
1. Install UiPath Studio Community Edition (https://www.uipath.com/community).
2. Install Google Drive for Desktop and let it mount your Drive (e.g. drive `G:`). Create folder `G:\My Drive\irrs-watch`.
3. On your Gmail account, enable 2-Step Verification then generate an App Password under Security > App passwords (label `IRRS-RPA`).
4. Copy `uipath/config.json.example` to `uipath/config.json`. Fill in REAL values:
   - `watch_folder` -> the absolute path to your `G:\My Drive\irrs-watch` folder
   - `processed_folder` -> the absolute path to `G:\My Drive\irrs-watch\processed`
   - `screenshots_folder` -> the absolute path to `<repo>/uipath/rpa_screenshots`
   - `email_from`, `email_to` -> your Gmail address
   - `email_app_password` -> the app password from step 3 (with spaces or without — both accepted)
5. Confirm `uipath/.gitignore` contains `config.json` so the secrets file is NOT committed.
6. Copy the three demo files from `uipath/test_files/` into your watch folder.
7. Start the IRRS backend in a terminal:
   ```
   cd irrs-backend && source venv/bin/activate && uvicorn main:app --reload --port 8000
   ```
8. Confirm http://localhost:8000/docs loads.

RUN 1 — happy path (3 created):
9. Open UiPath Studio. Open the project at `<repo>/uipath/`.
10. Open `Main.xaml`. Click `Run`.
11. Watch the Output panel: you should see three POST /api/incidents calls succeed, three /status PATCH calls, and a final SMTP send.
12. Confirm in the IRRS web app:
    - Visit http://localhost:3000/incidents — three new incidents with status `reviewed` and source `rpa`.
    - Visit http://localhost:3000/logs (after Plan 02-02 ships) OR query `curl -H "Authorization: Bearer <admin_token>" http://localhost:8000/api/logs` — at least 9 log entries with one shared `run_id`, actions `create` x3 and `status_update` x3, all `status: success`.
13. Check your Gmail inbox: a plain-text email titled `DHL IRRS RPA Run Summary — <run_id>` with body `Created: 3 / Duplicates: 0 / Failed: 0`.
14. Confirm the three .txt files have moved from the watch folder into `watch/processed/`.

RUN 2 — duplicate path (1 skip):
15. Copy `duplicate_test.txt` BACK from `processed/` into the watch folder.
16. Click `Run` again in UiPath Studio.
17. Confirm: Logs API has a new run with `action: skip_duplicate`, `status: skipped`. No new incident created in /incidents. Email body shows `Created: 0 / Duplicates: 1 / Failed: 0`.

RUN 3 — error path (1 failure):
18. Place a malformed file in the watch folder. Easy options: a zero-byte `bad.txt` (`type nul > bad.txt` on Windows) OR a 50MB file padded with zeros to trigger a server error.
19. Click `Run` again.
20. Confirm: a screenshot file appeared in `uipath/rpa_screenshots/` with name pattern `<run_id>_bad.txt.png`. Logs API has a new entry with `action: error`, `status: failed`, `screenshot_path` populated. Bot did NOT abort — it sent the summary email with `Failed: 1`.

If any of the three runs misbehaves, capture the UiPath Output panel text and the failing API response from the Studio HTTP Request output, then describe the problem in your resume signal so the executor can patch Main.xaml.
  </action>
  <verify>
    <automated>MISSING — checkpoint is human-verify only. Automated check: confirm scaffold integrity before user runs Studio: `test -f uipath/Main.xaml && test -f uipath/config.json.example && grep -q '^config.json$' uipath/.gitignore && python -c "import xml.etree.ElementTree as ET; ET.parse('uipath/Main.xaml')"`. Live runtime verification (Studio + Drive + Gmail) requires the user — Studio cannot be invoked headlessly here.</automated>
    <human>User runs Run 1, Run 2, Run 3 per the action steps above and types `approved` if all three pass.</human>
  </verify>
  <acceptance_criteria>
    - File `uipath/config.json` exists locally on the user's machine (NOT in git — confirmed by `uipath/.gitignore` containing `config.json`)
    - Run 1 result confirmed by user: 3 incidents created in IRRS web app with status `reviewed`, source `rpa`
    - Run 2 result confirmed by user: 1 skip_duplicate log entry visible, no new incident
    - Run 3 result confirmed by user: 1 screenshot saved under `uipath/rpa_screenshots/`, 1 error log entry with non-null `screenshot_path`
    - Three summary emails received in user's Gmail inbox with subject prefix `DHL IRRS RPA Run Summary —`
    - User explicitly types `approved` (or describes specific issues for follow-up)
  </acceptance_criteria>
  <resume-signal>Type `approved` to confirm all three runs passed. Otherwise paste the failing Output panel excerpt and which run (1/2/3) failed.</resume-signal>
  <done>End-to-end demo flow validated against live backend. RPA-01 through RPA-07 all observable: file detection, dedup skip, incident create with attachment, status advance, every action logged with shared run_id, screenshot on failure, summary email sent.</done>
</task>

</tasks>

<verification>
After Task 3 returns `approved`:
- `git status` shows new files only under `uipath/` (no backend or frontend changes)
- `git ls-files uipath/` does NOT include `uipath/config.json` (must be gitignored)
- `python -c "import xml.etree.ElementTree as ET; ET.parse('uipath/Main.xaml')"` exits 0
- `python -c "import json; json.load(open('uipath/project.json'))"` exits 0
- `python -c "import json; json.load(open('uipath/config.json.example'))"` exits 0
- All five RPA endpoints referenced in Main.xaml (verified by grep in Task 2 acceptance)
- Roadmap success criteria 1–5 all confirmed by Task 3 manual run
</verification>

<success_criteria>
- All 7 RPA requirements (RPA-01 through RPA-07) demonstrated in a live run
- Workflow runs end-to-end without manual intervention between files
- One failed file does not abort the run (graceful per-file Try/Catch)
- Secrets stay out of git (config.json gitignored)
- Demo is repeatable: moving files back out of `watch/processed/` resets the test
</success_criteria>

<output>
After completion, create `.planning/phases/02-rpa-automation/02-01-SUMMARY.md` documenting:
- Files created under `uipath/`
- Final config.json schema (sanitized — no real secrets)
- Demo script (Run 1 / Run 2 / Run 3 expected outcomes)
- Known limitations (8h JWT expiry, single-token-per-run)
- Screenshot of UiPath Studio with the workflow open (optional, attach to summary or note path)
</output>
