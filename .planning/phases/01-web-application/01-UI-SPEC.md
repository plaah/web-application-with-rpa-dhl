---
phase: 1
slug: web-application
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-04
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for the DHL IRRS web application frontend.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none | No components.json found; fresh Next.js project |
| Preset | not applicable | shadcn not initialized |
| Component library | none — hand-rolled with Tailwind | RESEARCH.md: Tailwind CSS 4.x scaffolded via create-next-app |
| Icon library | lucide-react | Default: pairs with Next.js/Tailwind; no icon library declared in PRD |
| Font | Inter (system default via next/font) | Default: professional, legible at small sizes |

**shadcn gate decision:** No `components.json` found. Project deadline is 11 days — skipping shadcn init to avoid preset overhead. Components are hand-rolled with Tailwind. Registry safety gate: not applicable.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, tag chip inner padding, badge padding |
| sm | 8px | Input inner padding (vertical), compact row gaps |
| md | 16px | Default element spacing, card inner padding, form field gaps |
| lg | 24px | Section padding, page header bottom margin |
| xl | 32px | Layout column gaps, card-to-card gaps |
| 2xl | 48px | Major section breaks, page top padding |
| 3xl | 64px | Page-level vertical rhythm (login page centering) |

Exceptions:
- Touch targets for status action buttons: minimum 44px height (accessibility)
- Navbar height: 56px (fixed, 14px increment — nearest 4px multiple above standard 48px to fit logo + nav items)
- Table row height: 48px minimum for readable data rows

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px | 400 | 1.5 | Table cell text, form labels, description text |
| Label | 12px | 600 | 1.4 | Status badges, table column headers, tag chips, nav items |
| Heading | 20px | 600 | 1.3 | Page titles, card section headings, modal titles |
| Display | 28px | 600 | 1.2 | Dashboard stat numbers (incident counts), login page title |

All text uses Inter (next/font/google). Two weights only: 400 (normal) and 600 (semibold).

**Mapping to Tailwind classes:**
- Body: `text-sm font-normal leading-relaxed` (14px / 400 / 1.5)
- Label: `text-xs font-semibold leading-snug` (12px / 600 / 1.4)
- Heading: `text-xl font-semibold leading-snug` (20px / 600 / 1.3)
- Display: `text-3xl font-semibold leading-tight` (28px / 600 / 1.2)

---

## Color

| Role | Hex | Tailwind Class | Usage |
|------|-----|----------------|-------|
| Dominant (60%) | #F9FAFB | `bg-gray-50` | Page background, main content surface |
| Secondary (30%) | #FFFFFF | `bg-white` | Cards, sidebar, nav bar, table rows, modals |
| Accent (10%) | #D40511 | `bg-red-600` | See reserved list below |
| Destructive | #DC2626 | `bg-red-600` | Delete incident button + confirmation only |
| Border | #E5E7EB | `border-gray-200` | Card borders, table dividers, input borders |
| Text primary | #111827 | `text-gray-900` | All headings and body copy |
| Text muted | #6B7280 | `text-gray-500` | Placeholder text, secondary metadata, timestamps |

**Accent (#D40511 — DHL Red) reserved for:**
- Primary CTA buttons ("Save Incident", "Upload File", "Apply Filters")
- Navbar logo/brand mark
- Active nav item left border indicator
- Pagination "current page" indicator
- Never used on status badges (status badges use their own semantic colors — see below)

**Status badge colors (locked by NFR-14):**

| Status | Background | Text | Tailwind |
|--------|-----------|------|---------|
| Draft | #F3F4F6 | #374151 | `bg-gray-100 text-gray-700` |
| Reviewed | #DBEAFE | #1D4ED8 | `bg-blue-100 text-blue-700` |
| Published | #DCFCE7 | #15803D | `bg-green-100 text-green-700` |

**Destructive color:** `#DC2626` (`text-red-600` / `bg-red-50 border-red-200`) — used exclusively on:
- "Delete Incident" button (admin only, Draft status only)
- Delete confirmation dialog destructive button

---

## Component Inventory

Components required by this phase (hand-rolled with Tailwind):

| Component | File | States | Notes |
|-----------|------|--------|-------|
| Navbar | `components/Navbar.tsx` | default, mobile-hidden | Logo left, nav links center, user+logout right |
| StatCard | `components/StatCard.tsx` | default, loading skeleton | Dashboard count cards |
| StatusBadge | `components/StatusBadge.tsx` | draft, reviewed, published | Pill shape, label weight |
| FilterBar | `components/FilterBar.tsx` | collapsed, expanded, active | Status dropdown, tag, date, keyword search |
| IncidentTable | `components/IncidentTable.tsx` | empty, loading, populated, paginated | 20 rows/page |
| IncidentForm | `components/IncidentForm.tsx` | blank, prefilled (from file extract), submitting, error | Controlled inputs |
| FileDropzone | `components/FileDropzone.tsx` | idle, drag-over, uploading, extracted, error | Accept .txt .pdf .docx |
| PaginationBar | `components/PaginationBar.tsx` | default | Previous/Next + page number |
| TabStrip | `components/TabStrip.tsx` | Details active, Files active, History active | Incident detail page |
| VersionHistoryList | `components/VersionHistoryList.tsx` | empty, populated | Timeline list of status changes |
| ConfirmDialog | `components/ConfirmDialog.tsx` | default, destructive | Used for delete and publish lock |

---

## Page Layout Contracts

### /login
- Centered card, 400px wide max, vertically centered on full-height page
- DHL Red header band at top with logo
- Username + password inputs stacked, "Sign In" CTA full-width
- Primary focal point: "Sign In" CTA button (full-width, accent red, bottom of card)

### /dashboard
- Top: 3 StatCards side by side (Draft / Reviewed / Published counts)
- Below: "Recent Incidents" table, last 10, no pagination
- Right sidebar: NOT required (keep single column, 960px max content width)
- Primary focal point: 3 StatCard numbers (Display size, top of page, above the fold)

### /upload
- Left column (60%): FileDropzone + extracted text preview textarea
- Right column (40%): IncidentForm (title, description pre-filled from extract, priority, category, tags)
- Single "Save Incident" CTA at bottom of form
- Primary focal point: FileDropzone (left column, top — first user action in the workflow)

### /incidents
- FilterBar at top (full width)
- IncidentTable below
- PaginationBar at bottom right
- Primary focal point: IncidentTable (full-width, dominant content area below FilterBar)

### /incidents/[id]
- Breadcrumb: Dashboard > Incidents > {title}
- Title + StatusBadge + status action button row
- TabStrip: Details | Files | History
- Tab content below
- Primary focal point: Status action button row (title + badge + CTA — top of content, drives next user action)

---

## Interaction Contracts

### Status Workflow Buttons
- Current status **Draft**: show "Mark as Reviewed" button (accent red, 44px height)
- Current status **Reviewed**: show "Publish Incident" button (accent red, 44px height)
- Current status **Published**: show no action button; show locked banner "This incident is published and cannot be edited."
- Admin + Draft: show "Delete Incident" button (destructive red, text only or ghost variant)

### File Upload Flow
1. User drops or selects file → dropzone shows filename + spinner
2. POST /api/files/extract → extracted text appears in textarea below dropzone
3. "Use extracted text" button pre-fills description field in form
4. If extract fails → dropzone shows error message (red border + error copy)

### Filter Bar
- All filters apply on change (no "Apply" button needed — each input triggers refetch)
- Active filter count shown as badge on FilterBar toggle button when collapsed

### Pagination
- Previous/Next buttons + "Page N of M" label
- Clicking page number not required — Previous/Next only

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — upload page | "Save Incident" |
| Primary CTA — login page | "Sign In" |
| Primary CTA — status advance (Draft) | "Mark as Reviewed" |
| Primary CTA — status advance (Reviewed) | "Publish Incident" |
| Empty state heading — incident list | "No incidents found" |
| Empty state body — incident list (no filters) | "No incidents have been created yet. Upload a file or create one manually." |
| Empty state body — incident list (filters active) | "No incidents match your filters. Try adjusting your search or clearing filters." |
| Empty state — version history tab | "No status changes recorded yet." |
| Empty state — files tab | "No files attached to this incident." |
| Published lock banner | "This incident is published and cannot be edited." |
| Error — file extraction failed | "File extraction failed. Check that the file is a valid PDF, DOCX, or TXT and try again." |
| Error — save incident failed | "Failed to save incident. Check your connection and try again." |
| Error — login failed | "Invalid username or password." |
| Error — status transition failed | "Status update failed. Refresh the page and try again." |
| Destructive confirmation — delete | "Delete this incident? This action cannot be undone." |
| Destructive CTA in dialog | "Delete Incident" |
| Destructive cancel | "Cancel" |
| Loading state — table | "Loading incidents…" |
| Loading state — file extract | "Extracting text…" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not applicable |
| Third-party | none | not applicable |

No third-party component registries. All UI is hand-rolled Tailwind. lucide-react installed via npm (not a shadcn registry block).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
